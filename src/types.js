// This file is no longer needed as we are using plain JavaScript/React without TypeScript.
// Keeping it empty to prevent import errors if there are lingering references,
// though we will remove references in other files.
export const GameState = {
  START: 'START',
  PLAYING: 'PLAYING',
  GAME_OVER: 'GAME_OVER',
};
